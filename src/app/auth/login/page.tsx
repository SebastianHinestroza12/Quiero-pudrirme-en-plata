import React, { Fragment } from "react";
import { LoginUser } from "@/components/Auth/Login";

const Login = () => {
  return (
    <Fragment>
      <LoginUser />
    </Fragment>
  );
};

export default Login;
